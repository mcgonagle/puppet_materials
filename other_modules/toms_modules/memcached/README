Puppet module: memcached

# Written by Tom McGonagle #
# mcgonagle@gmail.com
# Google Voice (617)229-5185
# http://www.puppetll.com


DESCRIPTION:
This module installs and manages memcached
Standard and extended classes are available.
Decide and customize how you want to define configation files: static files, templates, infile modifications...


USAGE:
# Standard Classes 
include memcached              # Install and run memcached with standard configurations.
include memcached::disable     # Disable memcached service.
include memcached::disableboot # Disable memcached service at boot time, but don't stop if it's running.
include memcached::absent      # Remove memcached package.

DEPENDENCIES:
